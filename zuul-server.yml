server:
  port: 8080 #the default port used for zuul service
  
zuul:
   igoredServices: '*'
   routes:
      echi-service:
         path: /echo/**
         serviceId: echo-service
         strip-prefix: false
 
ribbon:
   eureka:
      enabled: false
 
weather-service:
   ribbon:
      listOfServers: localhost:8091
        
